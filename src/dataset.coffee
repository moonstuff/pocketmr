# A dataset, an encapsulation of some amount of data.
#
# It simply has a relative path, a worker can turn this into and absolute path.
class DataSet

  constructor: (@path) ->

exports.DataSet = DataSet
